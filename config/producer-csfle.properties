# Producer for the CSFLE topic — values templated from .env via envsubst.
bootstrap.servers=${BOOTSTRAP_SERVERS}
security.protocol=SASL_SSL
sasl.mechanism=PLAIN
sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="${KAFKA_API_KEY}" password="${KAFKA_API_SECRET}";

schema.registry.url=${SR_URL}
basic.auth.credentials.source=USER_INFO
basic.auth.user.info=${SR_API_KEY}:${SR_API_SECRET}
use.latest.version=true
auto.register.schemas=false

# FieldEncryptionExecutor handles BOTH field-level (ENCRYPT) and payload-level
# (ENCRYPT_PAYLOAD) rules — the framework dispatches by rule type internally.
# TODO verify on first ENCRYPT_PAYLOAD produce; if it errors with "no executor",
# add: rule.executors=field,payload + .payload.class=PayloadEncryptionExecutor
rule.executors=_default_
rule.executors._default_.class=io.confluent.kafka.schemaregistry.encryption.FieldEncryptionExecutor
